export const actionGenerationPromptNotes = `
  Only return the JSON object, no extra text or formatting.
  Your response must be in valid JSON format without any additional characters like backticks (\`\`\`json or similar).
`;
